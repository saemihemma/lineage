# LINEAGE Game Dependencies
# Core game logic (no external dependencies beyond standard library)

# API Client (for leaderboard and telemetry)
# UPGRADED: Now using httpx for async/await support
httpx>=0.27.0

# Backend API (FastAPI)
fastapi>=0.109.0
uvicorn>=0.27.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.0.0  # Coverage reporting
httpx>=0.27.0  # For testing async HTTP
hypothesis>=6.0.0  # Property-based testing

